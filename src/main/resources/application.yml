spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://db:3306/to2
    username: to2
    password: to2
  jpa:
    properties:
      hibernate:
        jdbc:
          time_zone: UTC

recaptcha:
  siteKey: #
  privateKey: #
  active: false
